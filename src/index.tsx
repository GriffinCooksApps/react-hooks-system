import * as React from 'react';
import Provider from ',/Provider';

import hookSystemManager;

const ReactHookSystem = ( ) => {
	hookSystemManager.set();

}

export const Provider;



export default ReactHookSystem;
